terraform {
  backend "http" {
    address = "http://backend:8300/state" 
    lock_address = "http://backend:8300/lock"
    unlock_address = "http://backend:8300/lock"
    lock_method = "POST"
    unlock_method = "DELETE"
    username = "terraform"
    skip_cert_verification = true
  }
}

resource "local_file" "test" {
  content = "fooSTRING"
  filename = "${path.module}/test.txt"
}

